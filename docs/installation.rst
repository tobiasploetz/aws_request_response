.. highlight:: shell

============
Installation
============


Stable release
--------------

To install aws_request_response, run this command in your terminal:

.. code-block:: console

    $ pip install aws_request_response

This is the preferred method to install aws_request_response, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for aws_request_response can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/tobiasploetz/aws_request_response

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/tobiasploetz/aws_request_response/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/tobiasploetz/aws_request_response
.. _tarball: https://github.com/tobiasploetz/aws_request_response/tarball/master
